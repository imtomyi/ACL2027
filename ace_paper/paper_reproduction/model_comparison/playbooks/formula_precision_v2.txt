## OUTPUT FORMAT
[fmt-00001] Return `final_answer` as a plain base-10 number only. Do not include a percent sign, currency symbol, unit label, explanatory text, or thousands separators.
[fmt-00002] Keep intermediate calculations at full precision. Round only the final result to the nearest hundredth when needed.
[fmt-00003] Produce one valid JSON object with exactly the keys `reasoning`, `bullet_ids`, and `final_answer`.

## FORMULA SELECTION
[sel-00004] Identify the named financial metric before calculating. Apply its metric-specific formula and output convention below. A metric-specific rule always overrides a generic percentage convention.
[sel-00005] Convert percentage inputs such as 13% and 3% to decimal operands 0.13 and 0.03 before substituting them into formulas.

## OPERATING MARGIN
[om-00006] Operating Margin = (Operating Income / Revenue) * 100. Return percentage points without a percent sign. Thus 45000 / 300000 = 0.15 must be returned as 15.0.

## DIVIDEND DISCOUNT MODEL
[ddm-00007] For the constant-growth Dividend Discount Model, current stock price P0 = D1 / (r - g), where D1 is next period's dividend, r is the required return as a decimal, and g is the growth rate as a decimal.
[ddm-00008] Check that r is greater than g. For D1 = 4, r = 0.13, and g = 0.03, calculate 4 / (0.13 - 0.03) and return 40.0.

## DIVIDEND YIELD
[dy-00009] In this benchmark, Dividend Yield = Annual Dividends per Share / Price per Share and is returned as a decimal ratio, not percentage points. Do not multiply by 100.
[dy-00010] For annual dividends of 0.90 and a share price of 18, calculate 0.90 / 18 and return 0.05, not 5.0.

## UNIT AND SCALE CHECKS
[unit-00011] Normalize magnitude words before calculating: thousand means 1000, million means 1000000, and billion means 1000000000.
[unit-00012] Convert operands to compatible units before applying a formula.

## FINAL VERIFICATION
[chk-00013] Verify the named metric, selected formula, denominator, input percentage conversion, benchmark output convention, and final rounding before answering.
[chk-00014] Include only bullet IDs that actually appear in this playbook. Never invent placeholder IDs.
