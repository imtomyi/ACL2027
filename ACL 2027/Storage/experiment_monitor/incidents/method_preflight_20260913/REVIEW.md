# Method preflight review — 2026-09-13

The GoEmotions ICL runner stayed active throughout this review. No model request, optimizer search, paused experiment resume or protocol reduction was performed for testing. Software fixtures were isolated from experiment results.

## Findings and repairs

1. ACE with and without reference feedback raised AttributeError when the reflector returned a JSON array or null. This was reproduced with a software fixture. Invalid JSON or a non-object reflection now retains the previous playbook and next bullet ID with the explicit status `retained_previous_invalid_reflection`; no curator call is made for that invalid reflection. Valid object responses follow the existing prompt and update path.
2. The paired reference validator raised AttributeError for a non-object annotation, escaping its bounded technical validation retry handler. It now rejects the value with the validation exception already handled by that loop. No annotation correctness selection was added.
3. The paired DC curator cap was documented as 2,048 tokens. The actual vendored cumulative workflow doubles the 2,048 generator cap to 4,096 for curation; the existing official-workflow adapter test confirms this. Documentation and frozen metadata now reflect 4,096. No runtime generation budget was changed.

Both paused runs have explicit amendment records. Original configuration, changed source files and affected implementation snapshots are preserved under `before/` and `snapshot_before/`. The old GT-free run's 28 predictions and the paired run's 10 reference annotations retain their hashes. Both freeze checks pass. The active GoEmotions source hashes remain identical to its frozen configuration.

## Validation

- Dreaddit provider/DC/metric contracts: 8 passed.
- GoEmotions metric, append recovery and judge isolation: 4 passed.
- GoEmotions optimizer wiring: 4 passed. These instantiate the installed MIPROv2/GEPA classes with frozen options, check multi-label DSPy provider output and serialization, verify train/dev-only optimizer inputs, test exact-match feedback, and verify reuse of a completed compiled artifact.
- GT-free ACE contracts: 2 passed.
- Paired feedback contracts: 7 passed, including invalid reflection handling, reference validation, curator-only feedback, offline test-label isolation and online prediction-before-feedback order.

Total: 25 software tests passed. No new real-corpus score was generated by these checks. Full optimizer searches and ACE/DC model behavior are not guaranteed by software-only tests.

## Remaining runtime considerations

MIPROv2 and GEPA retain `auto=heavy` and can require many model calls. The installed MIPROv2 implementation limits its heavy-mode internal validation selection to 1,000 records even though the wrapper supplies the full official dev set. GEPA uses its own heavy budget. This is upstream behavior, not a new subset chosen during this review.

GEPA documents resume from the same log directory. The inspected MIPROv2 search creates an in-memory study and saves candidate programs/logs; restoring a finished compiled program is tested, but automatic restoration of its intermediate search state is not established. Before interrupting MIPROv2, inspect completed calls and optimizer progress. Do not describe response-cache reuse as complete optimizer-state recovery.

Context overflow, truncated output and unresolved judgments remain explicit failures/limitations rather than silently changing budgets or scoring missing data as zero. Further runtime failures can still occur; the five-minute recovery monitor remains enabled.
