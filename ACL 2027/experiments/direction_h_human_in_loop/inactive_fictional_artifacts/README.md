# Inactive fictional/synthetic artifacts

No fictional or synthetic data is authorized for the active Direction H study.
This folder is an index only; it contains no item data.

Earlier prepared artifacts remain physically in `pilot/`,
`companion_j_bridge/`, their related coordinator templates, and their
synthetic-specific scripts/schemas. Their shared qualification dependency was
moved to
`Storage/synthetic-results/model-qualification/20260825_synthetic_qualification/source-package/`;
the resulting legacy paths are intentionally non-runnable. The remaining files
are retained solely as an audit trail. They are not active, may not be validated or run, and
must not supply practice examples, qualifications, ratings, revisions, repair
assessments, or empirical claims.

These artifacts must not be reactivated for manuscript work. The active study
uses only approved real-corpus inputs after every governance gate passes.
