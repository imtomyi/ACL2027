# Real-data run area

No Direction H real-data run exists. This directory is reserved for approved
run metadata and result references after all gates pass; it must never contain
raw transcripts, copied protected records, or unapproved excerpts.

Do not create a run directory from a technically valid dataset build alone.
The run must cite the current evidence-backed readiness record, frozen study
manifest, exact real-data runtime version, reviewer permissions, and approved
model endpoint. Until then, the empty state is intentional.
