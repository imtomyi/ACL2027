# Revision outputs

No output exists. Retain every allocated case and its single frozen attempt.
Runtime, timeout, parse, schema, and integrity failures remain in the pipeline
ITT denominator with primary success equal to zero. Never choose a best output,
redact a completed output, or silently substitute another attempt.
