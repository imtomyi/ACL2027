# Raw revision responses

No response exists. The approved frozen runtime writes one raw response per
allocated case here. The Direction H recorder hashes and validates that exact
file but never calls a model, retries, repairs formatting, or selects another
attempt. Keep this directory coordinator-restricted.
