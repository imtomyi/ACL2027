# De-identified working data

Store analysis copies here only after applying and logging the approved privacy transformations. Treat them as restricted by default: removing usernames does not make verbatim Reddit text anonymous when a phrase can still be searched or linked to its author.
